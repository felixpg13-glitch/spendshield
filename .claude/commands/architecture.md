---
description: Show SpendShield architecture & security model
---

输出 SpendShield 架构速览: 三层地图(Identity/Delegation → Policy control plane(我们) → Execution safety), 双模式(advisory guard + enforcement enforce.py), 不做清单(identity/crypto delegation/reputation), 关键文件(guard.py / enforce.py / audit.py / mcp_server.py / policy/lifecycle.py)。参考 docs/COMPETITIVE_POSITION.md。
